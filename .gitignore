# PyCharm stuff which will be custom per-person
/.idea/*

# Data which is generated our data preparation scripts (and does not need to be tracked)
/datasets/raw/
/datasets/cleaned/
/datasets/configs/

# The restults database, which is generated by our ML analyses
/results.db

# Python notebook checkpoints of questionable utility
/**/.ipynb_checkpoints/

# Path to the Modular Optuna library, currently stored within the repo's direcotry for ease of access. LIKELY TO CHANGE SOON(tm)
/modular_optuna_ml/*

# Miscellaneous Testing stuff that should not be save to the git repo
/results_backup.db
/presplit.tsv
/debug/
/testing/
