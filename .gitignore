/presplit.tsv
/debug/*
/testing/output/output.db
/testing/private/*

# PyCharm stuff which will be custom per-person
/.idea/*

# Data which is generated our scripts which does not need to be tracked
/datasets/cleaned/**/*.csv
/datasets/cleaned/**/*.tsv
/datasets/cleaned/**/*.json
/datasets/raw/**/*.csv
/datasets/raw/**/*.tsv
/datasets/raw/**/*.json

# The restults database, which is generated by our ML analyses
/results.db

# Python notebook checkpoints of questionable utility
/**/.ipynb_checkpoints/

# Path to the Modular Optuna library, currently stored within the repo's direcotry for ease of access. LIKELY TO CHANGE SOON(tm)
/modular_optuna_ml/*
